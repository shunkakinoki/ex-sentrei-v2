import DashboardScreen from "@/components/DashboardScreen";

const Dashboard = (): JSX.Element => {
  return <DashboardScreen articles={[]} current={0} total={0} namespaceId="" />;
};

export default Dashboard;
