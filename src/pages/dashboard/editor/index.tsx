import {NextPage} from "next";

import EditorScreen from "@/components/EditorScreen";

const Editor: NextPage = () => {
  return <EditorScreen articleId="" namespaceId="" />;
};

export default Editor;
